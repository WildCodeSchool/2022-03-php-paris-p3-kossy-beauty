<form method="post" action="{{ path('app_conversation_delete', {'id': conversation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conversation.id) }}">
    <button class="btn">Delete</button>
</form>
