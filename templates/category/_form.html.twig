{{ form_start(form) }}
<p class="mt-3 mb-0">{{ form_label(form.name, 'Nom de la catégorie :')}} {{ form_widget(form.name) }}</p>
<button class="btn btn-success p-2 mt-2">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
