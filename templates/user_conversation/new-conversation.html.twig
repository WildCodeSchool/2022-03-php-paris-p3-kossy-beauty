{% extends 'base.html.twig' %} {% block title %}Create Conversation !{% endblock
%} {% block body %}

<div
    class="container w-50 mb-2 d-flex align-items-center justify-content-between"
>
    <a
        class="btn btn-outline-secondary"
        href="{{ path('app_my_conversations') }}"
        >Retour</a
    >
    <p class="m-0">Envoyer un message à {{ recipient.firstname }}</p>
</div>
<div class="container w-50 mt-3">
    {{ form_start(form) }}
    {{ form_widget(form.content, {'attr': {'class': 'form-control
                form-control-lg'
    }}) }}
    <button class="btn btn-success mt-2">
        {{ button_label|default('Envoyer') }}
    </button>

    {{ form_end(form) }}
</div>
{% endblock %}
