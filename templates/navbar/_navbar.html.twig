<nav class="row navbar navbar-expand-lg navbar-light bg-light py-3">

    <div class="col">
      <a class="navbar-brand" href="/"><img class="position-relative start-2 top-0 mt-0"src="{{ asset('build/images/LogosymbolKB1.e2883951.jpeg') }}" style="width:4rem" alt="Logo"></a>
    </div>
      
    <div class="col">
      {% if is_granted("ROLE_SUPER_ADMIN") %}
        <a class="ms-auto"href="{{ path('app_admin') }}">Super Admin Dashboard</a>
      {% endif %}
        <a href="{{ path('app_register_pro') }}"><i class="bi bi-plus-square me-2"></i>Devenir prestataire</a>
    </div>

    <div class="col">
      <div class="collapse navbar-collapse pt-0" id="navbarSupportedContent">
        <ul class="navbar-nav mb-2 mb-lg-0 text-center w-100">
          <li class="nav-item position-relative me-3">
            <span class="d-inline-block" id="tooltip-geolocation" tabindex="0" data-bs-toggle="tooltip" data-bs-placement="bottom" title="Géolocalisation automatique">
              <button class="btn btn-primary" type="button" disabled>
                <i class="bi bi-geo-alt-fill"></i> 
                {{ geolocation.district }}
              </button>
            </span>
          </li>
          <li class="nav-item position-relative me-3">
            <i class="bi bi-chat-left-text p-0"></i>
            <a class="nav-link active stretched-link p-0" aria-current="page" href="{{ path('app_my_conversations') }}">Messages</a>
          </li>
          <li class="nav-item position-relative me-3">
            <i class="bi bi-heart p-0"></i>
            <a class="nav-link active stretched-link p-0" aria-current="page" href="#"></i>Favoris</a>
          </li>
          {% if is_granted("ROLE_USER") %}
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown"
                aria-expanded="false">
                <i class="bi bi-person"></i>Mon compte
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                <li><a class="dropdown-item" href="{{ path('app_user_show', {id: app.user.id}) }}">Voir mon profil</a></li>
                <li>
                  <hr class="dropdown-divider">
                </li>
                <li><a class="dropdown-item" href="/logout">Se déconnecter</a></li>
              </ul>
            </li>
          {% else %}
            <li class="nav-item position-relative">
              <i class="bi bi-person p-0"></i>
              <a class="nav-link active stretched-link p-0" aria-current="page" href="/login">Connexion</a>
            </li>
          {% endif %}
        </ul>
      </div>
    </div>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

</nav>
