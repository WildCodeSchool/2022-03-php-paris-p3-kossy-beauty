<nav class="navbar navbar-expand-lg navbar-light py-3 pb-1">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><img class="logo-navbar position-absolute ms-2 d-md-block pt-2"src="{{ asset('build/images/Kossy-08.png') }}" alt="Logo"></a>
    {# {{ render(controller(
        'App\\Controller\\SearchController::searchBar'
      )) }} #}
    <div class="collapse navbar-collapse pt-0 justify-content-end align-items-center" id="navbarSupportedContent">
      <ul class="navbar-nav rounded text-center position-absolute end-0 mt-2 mt-md-1 me-2 p-3 ps-4 p-md-0">
        {% if notification.notification is same as false %}
          <li class="nav-item rounded align-items-center position-relative me-3 p-2 text-start text-md-center color-red">
            <i class="bi bi-envelope-exclamation-fill me-1 fs-5"></i>
        {% else %}
          <li class="nav-item rounded align-items-center position-relative me-3 p-2 text-start text-md-center text-light w-100">
            <i class="bi bi-envelope-fill me-1 fs-5"></i>
        {% endif %}
            <a class="nav-link d-inline d-lg-block active stretched-link p-0 text-light" aria-current="page" href="{{ path('app_my_conversations') }}">Messages</a> 
          </li>
          <li><hr class="dropdown-divider"></li>
          {# <li class="nav-item rounded align-items-center position-relative me-3 p-2 text-start text-md-center w-100">
            <i class="bi bi-star-fill me-1 fs-5"></i>
            <a class="nav-link d-inline d-lg-block active stretched-link p-0" aria-current="page" href="#"></i>Favoris</a>
          </li> 
          <li><hr class="dropdown-divider"></li> #}
          {% if is_granted("ROLE_USER") %}
            {% include "navbar/_navLoggedUser.html.twig" %}
          {% else %}
            <li class="nav-item rounded align-items-center position-relative me-3 p-2 text-start text-md-center w-100 text-light">
              <i class="bi bi-person-bounding-box me-1 fs-5"></i>
              <a class="nav-link d-inline d-lg-block active stretched-link p-0 text-light" aria-current="page" href="{{ path('login') }}"></i>Connexion</a>
            </li>
          {% endif %}
      </ul>
    </div>
  </div>
  <button class="navbar-toggler position-absolute end-0 me-4 mt-2" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <i class="bi bi-list text-white fw-bolder fs-2"></i>
  </button>
</nav>
