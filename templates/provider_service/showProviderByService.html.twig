{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h1>Service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ service.name }}</td>
            </tr>
        </tbody>
    </table>
    {% for providerService in providerServices %}
    {% for provider in provider.getProviderById(providerService.provider.id) %}
        {{ provider.firstname }}
        {% if is_granted('ROLE_PROVIDER') %}
            {% else %}
            <a href="{{ path('app_create_conversation', {id: provider.id, service: service.name}) }}" class="btn btn-primary">Envoyer un message</a>
        {% endif %}
    {% endfor %}
    <p>{{ providerService.price}} € / {{ providerService.duration}} min</p>
    {% endfor %}
{% endblock %}
